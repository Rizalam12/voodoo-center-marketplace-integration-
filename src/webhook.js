const crypto=require("node:crypto"),fs=require("node:fs"),path=require("node:path"),FILE=path.join(process.cwd(),"data","processed-events.json");function verifySignature(raw,sig){const s=process.env.WEBHOOK_SECRET?.trim();if(!s||!sig)return false;const e=crypto.createHmac("sha256",s).update(raw).digest("hex"),a=Buffer.from(String(sig)),b=Buffer.from(e);return a.length===b.length&&crypto.timingSafeEqual(a,b)}function load(){try{return JSON.parse(fs.readFileSync(FILE,"utf8"))}catch{return{}}}function alreadyProcessed(id){return!!id&&Object.prototype.hasOwnProperty.call(load(),id)}function markProcessed(id){if(!id)return;fs.mkdirSync(path.dirname(FILE),{recursive:true});const d=load();d[id]=new Date().toISOString();fs.writeFileSync(FILE,JSON.stringify(d,null,2))}module.exports={verifySignature,alreadyProcessed,markProcessed};